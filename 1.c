#include<stdio.h>
int main(){
float width,height,area;
printf("Display area calculation of rectangle\n\n");
printf("Enter the width(m) :");
scanf("%f",&width);
printf("Enter the height(m):");
scanf("%f",&height);
area=width*height;
printf("Area of the rectangle:""%f",area);
return 0;
}
